//Declare an anonymous function which will run when the page is loaded$(function() {  //Add event listener to all li elements to be triggered on click  $('li').on('click', function() {    //When clicked animate the li clicked; change the following styles    $(this).animate({      //Make opacity 0 (disappear)      opacity: 0.0,      //Make fly right by 80 pixels      paddingLeft: '+=80'      //have the anmiation take 500 milliseconds, declare anonymous function    }, 500, function() {      //Remove the clicked li when animation finishes      $(this).remove();    });  });});